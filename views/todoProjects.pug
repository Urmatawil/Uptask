extends layout 

block content
    .principal-content
        h1 #{name} - #{project.name}

        .contenedor-acciones
            a(href=`/projects/edit/${project.id}` class="button") Edit Project
            button#eliminar-proyecto(type="button" class="button eliminar") Eliminar