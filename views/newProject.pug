extends layout 

include mixins/createProject

block content
    .principal-content
        h1 #{name}
        pre=vardump(project)


        if errors
            each error in errors 
                .alerta(class="error")= error.text
        +createProject(project)
            